import styled from "styled-components";

export const StyledNav = styled.div`
  position: absolute;
  z-index: 2;
  padding: 1.5rem;
  font-weight: 600;
  font-size: 2rem;
  font-family: monospace;
`;
